# base-babel

Boilerplate for babel

## Get started

- Clone this repo
- `cd` into the folder
- Run `./setup.sh`
- Replace `__YOUR_PACKAGE_NAME_HERE__` in `package.json`
- Code in `index.js`
- Run it with `npm start`
